<dl>
  <dt>Source</dt>
  <dd><em><%= document.source %></em></dd>

  <dt>Subject(s)</dt>
  <dd>
    <%# TODO: Refactor this into a partial %>
    <% document.subject.each do |sub| %>
      <%= link_to_facet(sub, Solrizer.solr_name("subject", :facetable)) %>
      <br />
    <% end %>
  </dd>
</dl>
